# ZAYDEN environment examples (copy to `.env` locally; never commit secrets)
#
# Optional: override paths for large artifacts kept outside the repo root.
# ZAYDEN_MODEL_ARCHIVE=C:\\path\\to\\gemma-2-2b-it-f32.zip
#
# Optional: future provider keys will be introduced behind explicit ADRs.
# ZAYDEN_OPENAI_COMPAT_BASE_URL=
# ZAYDEN_OPENAI_COMPAT_API_KEY=
