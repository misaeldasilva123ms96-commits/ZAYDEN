# `apps/cli` (scaffold)

Future home for a ZAYDEN-owned CLI entrypoint (if/when separated from library/runtime packages).

## Phase status

Intentionally empty beyond this README — no premature app wiring.
