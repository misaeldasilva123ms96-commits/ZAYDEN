# Provider gateway (scaffold)

This layer will host **adapters** that normalize provider calls behind stable ZAYDEN contracts.

## Phase 1 status

Scaffold only. No adapters implemented.

## Integration rule (charter)

Local GGUF assets (for example Gemma) must be reachable only through gateway adapters, not hardcoded paths scattered across runtime core.
